# Sanity (read-only, build-time). The project id is public and safe to expose.
# Copy to .env.local and fill in. Without these, the build uses the in-repo
# fallback content in src/constant/index.ts.
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
